export enum CSS2DObjectStateIndices {
  CAMERA_FAR,
  DISTANCE_TO_CAMERA_SQUARED,
  IS_RENDERED,
  TRANSLATE_X,
  TRANSLATE_Y,
  VERSION,
  VISIBLE,
  Z_INDEX,

  // This one NEEDS to be last to get the enum size correctly
  __TOTAL,
}
