<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="icon" href="/g/personalidol/assets/favicon.ico?{{{__CACHE_BUST}}}">
    <link rel="manifest" href="/g/personalidol/manifest.json?{{{__CACHE_BUST}}}">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <meta name="theme-color" content="#000000">
    <title>Personal Idol</title>
    <style>
      ::selection {
        background-color: white;
        color: black;
      }

      html, body {
        touch-action: none;
        -webkit-user-select: none;
        user-select: none;
      }

      body {
        -moz-osx-font-smoothing: grayscale;
        -webkit-font-smoothing: antialiased;
        background-color: black;
        margin: 0;
        overflow: hidden;
      }

      #canvas,
      #ui-root {
        left: 0;
        position: absolute;
        top: 0;
        will-change: height, width;
      }

      #ui-root {
        pointer-events: none;
      }

      .ui-placeholder-label {
        bottom: 2.4rem;
        color: white;
        font-family: sans-serif;
        font-size: 1rem;
        font-variant: small-caps;
        letter-spacing: 0.1ch;
        left: 1.6rem;
        line-height: 1rem;
        position: absolute;
        text-align: center;
        text-transform: lowercase;
      }

      .ui-placeholder-label.ui-placeholder-label--disabled {
        display: none;
      }
    </style>
  </head>
  <body>
    <div id="game-root">
      <canvas id="canvas"></canvas>
      <div id="ui-root">
        <pi-progress-manager-state class="ui-placeholder" id="ui-placeholder">
          <div class="ui-placeholder-label ui-placeholder-label--disabled" id="ui-placeholder-label">
            Loading ...
          </div>
          <noscript class="ui-placeholder-label">
            Error: You need to JavaScript to run this application.
          </noscript>
        </pi-progress-manager-state>
      </div>
    </div>
    <script>(function () {
      document.getElementById("ui-placeholder-label").classList.remove("ui-placeholder-label--disabled");
    }());</script>
    <script src="/g/personalidol/lib/index.js?{{{__CACHE_BUST}}}" type="module"></script>
  </body>
</html>
