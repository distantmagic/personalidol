// @flow

// Accepts async callback function and immediately returns cancel token

// const result = withCancelToken(props.loggerBreadcrumbs.add("DialogueQuery"), async function (cancelToken) {});

// maybe result can have some other fields like result.promise, result.callback
// etc

// export default function withCancelToken(): CancelToken;
