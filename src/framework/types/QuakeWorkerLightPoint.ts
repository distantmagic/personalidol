export type QuakeWorkerLightPoint = {
  readonly classname: "light_point";
  readonly color: string;
  readonly decay: number;
  readonly intensity: number;
  readonly origin: [number, number, number];
};
