metadata:

  start_message: start

messages:

  start:
    actor: Volo
    prompt: >
      Siema, {{ turn.initiator.name }}!
      Jam jest {{ message.actor }}!

  a1:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: not message.actor.ai()
    prompt: siema :D :D :D

  a11:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: true
    prompt: SIEMAAAAAAAAAAA {{ message.actor }}! :D :D :D

  a2:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: true
    prompt: Co tam???????????????? xD

  a3:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: true
    prompt: NARA :( :( :(

  b1:
    answer_to: a1
    actor: Volo
    prompt: Woooo!

  c1:
    answer_to: b1
    actor: "{{ message.key }}"
    prompt: Co?

  c2:
    answer_to: b1
    actor: "{{ message.key }}"
    prompt: Co?

  a5:
    answer_to: a1
    actor: Volo
    prompt: Hooooo!
