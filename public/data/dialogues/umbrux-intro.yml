metadata:

  start_message: start

messages:

  start:
    actor: Volo
    prompt: >
      Siema, {{ turn.initiator.name }}!
      Jam jest {{ message.actor }}!

  f1:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: not message.actor.ai()
    prompt: siema :D :D :D

  f11:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: true
    prompt: SIEMAAAAAAAAAAA {{ dialogue.start.actor }}! :D :D :D

  f2:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: true
    prompt: Co tam???????????????? xD

  f3:
    answer_to: start
    actor: "{{ message.key }}"
    pickable: true
    prompt: NARA :( :( :(

  f4:
    answer_to: f1
    actor: Volo
    prompt: Woooo!

  f5:
    answer_to: f1
    actor: Volo
    prompt: Hooooo!
